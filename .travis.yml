language: ruby
cache: bundler
script: 'bundle exec rake'
rvm:
  - 2.1.2
before_install:
  - gem update bundler
  - sudo apt-get -qq update
  - sudo apt-get install -y pdftk=2.01
notifications:
  email:
    recipients:
      - tomas.celizna@gmail.com
      - asger@8kilo.com
    on_failure: change
    on_success: never
